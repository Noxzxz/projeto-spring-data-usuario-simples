# Planejamento de Sprints
## Gamificação para Engajamento de Educação Continuada

**Arquitetura:** Java + Spring Boot + DDD + SOLID | **Frontend:** Angular | **Entrega:** 25/05/2025

**Equipe:** Ihago (SM + Backend) · Ihan (Backend) · Evelyn (Frontend) · Madu (Frontend Support)

---

## 0. Referência Arquitetural DDD

Todas as tasks deste documento referenciam a estrutura de pacotes, entidades, Value Objects, Use Cases e Domain Services definidos no Plano de Arquitetura DDD. A tabela abaixo é o mapa de referência rápida — use para verificar a qual camada e pacote cada artefato pertence.

| Bounded Context | Camada DDD | Classes / Interfaces Principais | Observação |
| :--- | :--- | :--- | :--- |
| usuario | domain.entity | Pessoa (abstract), Aluno\*, Professor, Administrador | Aluno é Aggregate Root; id: UUID |
| usuario | domain.valueobject | Email, NomeCompleto, CarteiraDigital | Java Records com validação no construtor |
| usuario | domain.repository | AlunoRepository, ProfessorRepository | Interfaces — sem dependência de Spring |
| usuario | domain.service | UpgradePlanoService | verificarEAplicarUpgrade() — RN004 |
| usuario | infrastructure.security | JwtAuthFilter, SecurityConfig, MfaService | Roles: ALUNO, PROFESSOR, ADMIN |
| usuario | interfaces.rest | AlunoController, AuthController | DTOs: AlunoResponseDTO, LoginResponseDTO |
| academico | domain.entity | Curso\*, Modulo, Aula, Matricula\*, Avaliacao, ProjetoFinal | \* = Aggregate Root do contexto |
| academico | domain.valueobject | NivelCurso, RespostaCorreta, MetricasDesempenho | Records imutáveis |
| academico | domain.enums | StatusMatricula, TipoConteudo, StatusCurso, ModalidadeCurso, StatusProjeto | StatusProjeto: AGUARDANDO/REVISAO/APROVADO |
| academico | domain.service | ProgressaoModularService, AprovacaoCursoService | RN011/RN024 centralizadas |
| academico | application | ConcluirModuloUseCase, MatricularAlunoUseCase, AvaliarProjetoUseCase | Orquestram domain services e repos |
| academico | interfaces.rest | CursoController, MatriculaController, ProjetoFinalController | Routes: /cursos, /matriculas, /projetos |
| financeiro | domain.entity | Assinatura\*, Pagamento, PagamentoPix/Boleto/Cartao | Plano está no Aluno (tipoPlano: TipoPlano enum) |
| financeiro | domain.valueobject | Valor | BigDecimal + moeda; valida >= 0 |
| financeiro | domain.service | AssinaturaService, PagamentoGateway (interface) | PagamentoGateway: anti-corrupção / port |
| gamificacao | domain.entity | Voucher, TransacaoMoeda | TransacaoMoeda imutável (auditoria — RNF008) |
| gamificacao | domain.service | RecompensaService, ConversaoMoedaService | RecompensaService: listener de CursoConcluídoEvent |
| gamificacao | application | ConverterMoedasUseCase, EmitirVoucherUseCase, UpgradeParaPremiumUseCase | UpgradePremium chama UpgradePlanoService |
| gamificacao | interfaces.rest | GamificacaoController | /gamificacao/moedas, /gamificacao/vouchers |
| engajamento | domain.entity | Forum\*, Topico, Comentario, EngajamentoAluno | EngajamentoAluno: ranking mensal — RN003 |
| engajamento | domain.service | RankingForumService, ModeracaoForumService | Moderação automática RF015; Ranking RF004 |

---

## 1. Backlog Completo

### 1.1 Epics e Bounded Contexts

| ID | Prioridade | Epic | Bounded Context | Owner | Status MVP |
| :--- | :--- | :--- | :--- | :--- | :--- |
| E1 | Must Have | Identidade e Autenticação | usuario | Toda equipe | Incluído no MVP |
| E2 | Must Have | Catálogo e Matrícula | academico | Toda equipe | Incluído no MVP |
| E3 | Must Have | Progressão Acadêmica | academico | Toda equipe | Incluído no MVP |
| E4 | Must Have | Atividades e Submissão | academico | Toda equipe | Incluído no MVP |
| E5 | Must Have | Gamificação e Recompensas | gamificacao | Toda equipe | Incluído no MVP |
| E6 | Should Have | Dashboard do Aluno | usuario / gamificacao | Evelyn + Ihan | Should Have |
| E7 | Could Have | Fórum Básico | engajamento | — | Could Have |
| E8 | Out of Scope | Pipeline Admin / Aprovação | academico | — | Fora do escopo |
| E9 | Out of Scope | Financeiro Real (Gateway) | financeiro | — | Fora do escopo |

---

### 1.1 Identidade e Autenticação [usuario]

| ID | Task | Owner | US | Critério de Aceitação + Teste Básico |
| :--- | :--- | :---: | :--- | :--- |
| T1.1 | Entidades Pessoa (abstract), Aluno, Professor, Administrador em `domain.usuario.entity` com herança JPA + Value Objects Email e NomeCompleto como Java Records | Ihago | USA01 | **Critérios de Aceitação:**<br>• Classe abstrata Pessoa com id UUID em `domain.usuario.entity`; herança `@Inheritance(JOINED)`.<br>• VOs Email e NomeCompleto como Java Records em `domain.usuario.valueobject` com validação no construtor.<br>• Aluno possui `tipoPlano: TipoPlano` (enum BASICO/PREMIUM) — sem entidade Plano separada.<br>• `AlunoRepository` interface declarada em `domain.usuario.repository` (dependência invertida — sem importar Spring).<br>• Implementação `AlunoRepositoryJpa` em `infrastructure.persistence.jpa` implementando a interface do domínio (padrão Adapter — DIP).<br><br>**Teste básico:** Aplicação sobe sem erros; DDL gera tabelas pessoa/aluno/professor/administrador com FK de herança. Persistir Aluno com Email VO → registro salvo; email inválido → exceção lançada no construtor do VO. |
| T1.2 | `infrastructure.security`: JwtAuthFilter, SecurityConfig com roles ALUNO/PROFESSOR/ADMIN, e geração/validação de token JWT | Ihago | USA02 | **Critérios de Aceitação:**<br>• `JwtAuthFilter` em `infrastructure.security` extrai e valida token JWT em toda requisição.<br>• `SecurityConfig` define roles ALUNO, PROFESSOR, ADMIN; rotas `/auth/**` são públicas, demais exigem autenticação.<br>• Token JWT contém claims: userId (UUID), email, role. Validade configurável via `application.properties`.<br>• Requisições sem token → 401. Requisições com role insuficiente → 403.<br><br>**Teste básico:** POST `/auth/login` → 200 + JWT. Request em rota protegida sem token → 401. Request com token de ALUNO em rota de ADMIN → 403. |
| T1.3 | API REST: POST `/auth/register` e POST `/auth/login` em `interfaces.rest.usuario.AuthController` + `AuthUseCase` em `application.usuario` | Ihago | USA01 | **Critérios de Aceitação:**<br>• `AuthController` em `interfaces.rest.usuario` delega para `AuthUseCase` em `application.usuario`.<br>• Register: valida Email VO (único no sistema), hash BCrypt via `infrastructure.security`, persiste via `AlunoRepository`.<br>• Login: carrega aluno por Email VO, valida senha, retorna JWT. Erros retornam mensagem legível sem vazar detalhes.<br>• Request/Response via DTOs (`RegisterRequestDTO`, `LoginResponseDTO`) em `interfaces.rest.usuario`.<br><br>**Teste básico:** POST `/auth/register` com dados válidos → 201. E-mail duplicado → 409. POST `/auth/login` correto → 200 + token. Senha errada → 401 com mensagem genérica. |
| T1.4 | Angular: módulo de roteamento, AuthGuard usando JWT claims, interceptor HTTP que injeta Bearer token | Evelyn | USA02 | **Critérios de Aceitação:**<br>• `AuthGuard` verifica token e role via `AuthService`; redireciona para `/login` se ausente ou expirado.<br>• `HttpInterceptor` injeta `Authorization: Bearer {token}` em todas as requisições autenticadas.<br>• Rotas com `canActivate: [AuthGuard]` para páginas protegidas; rota `/login` pública.<br><br>**Teste básico:** Acessar `/catalogo` sem login → redirect `/login`. Após login, todas requests HTTP têm header Bearer. Token expirado → redirect `/login` automático. |
| T1.5 | `AuthService` Angular: armazenamento de token, extração de claims JWT, métodos `isLoggedIn()` e `getCurrentUser()` | Madu | USA02 | **Critérios de Aceitação:**<br>• Token persistido em `localStorage`. `isLoggedIn()` verifica presença e validade (exp claim).<br>• `getCurrentUser()` decodifica JWT e retorna `{ id: UUID, email, role }`.<br>• `logout()` remove token e redireciona para `/login`.<br><br>**Teste básico:** Login → token em `localStorage`. Reload → ainda autenticado. `logout()` → token removido. Token expirado → `isLoggedIn()` retorna false. |
| T1.6 | Páginas Angular: Login e Cadastro com formulários reativos, validação inline e integração com AuthController | Evelyn | USA01 | **Critérios de Aceitação:**<br>• Formulários reativos validam: nome obrigatório, email formato válido, senha mínima 6 caracteres.<br>• Erros de validação exibidos inline por campo. Mensagens de erro da API exibidas em alerta global.<br>• Sucesso no cadastro → redirect `/login`. Sucesso no login → redirect `/catalogo`.<br><br>**Teste básico:** Submit com campos vazios → erros inline visíveis. Submit válido → chamada API. Resposta de e-mail duplicado → mensagem de erro exibida. |

---

### 1.2 Catálogo e Matrícula [academico]

| ID | Task | Owner | US | Critério de Aceitação + Teste Básico |
| :--- | :--- | :---: | :--- | :--- |
| T2.1 | `domain.academico.entity`: Curso (Aggregate Root), Modulo, Aula + enums StatusCurso, ModalidadeCurso, TipoConteudo em `domain.academico.enums` + VO NivelCurso | Ihan | USA03 | **Critérios de Aceitação:**<br>• Curso como Aggregate Root com métodos `adicionarModulo()` e `aceitaNovasMatriculas()`; atributos: `publicoAlvo`, `conhecimentosPrevios`, `status` (StatusCurso), `modalidade` (ModalidadeCurso), `maxAlunos`, `minAlunos`.<br>• Modulo possui `ordem: int` e referência a `Avaliacao` (entity separada para gabarito — RF029).<br>• Aula possui `tipoConteudo: TipoConteudo` (VIDEO, PDF, LINK, AULA_SINCRONA_GRAVADA), `ehSincrona`, `urlGravacao`.<br>• `NivelCurso` como VO (Java Record) em `domain.academico.valueobject`.<br>• `CursoRepository` interface em `domain.academico.repository`; implementação JPA em `infrastructure.persistence.jpa`.<br><br>**Teste básico:** DDL gera tabelas curso/modulo/aula com FKs corretas. Seed cria curso com 2 módulos e 3 aulas. `Curso.aceitaNovasMatriculas()` retorna false se inscritos >= maxAlunos. |
| T2.2 | `domain.academico.entity`: Matricula (Aggregate Root) com métodos de domínio `podeAvancarModulo()` e `concluir()` + `MatriculaRepository` interface | Ihan | USA05 | **Critérios de Aceitação:**<br>• Matricula como Aggregate Root com: `aluno` (UUID ref), `curso` (UUID ref), `status` (StatusMatricula), `moduloAtual`, `notaFinal`.<br>• Métodos de domínio: `podeAvancarModulo(ordemModulo)` implementa RN011/RN024; `concluir(notaFinal)` muda status; `verificarAprovacao()` retorna nota > 7.0.<br>• `MatriculaRepository` interface em `domain.academico.repository` com `countConcluidosByAluno(UUID)` para trigger de upgrade.<br>• Unique constraint em `(aluno_id, curso_id)` via `@Table(uniqueConstraints)`.<br><br>**Teste básico:** Matricular aluno → status EM_ANDAMENTO. Tentar segunda matrícula no mesmo curso → 409. `Matricula.verificarAprovacao()` com nota 8.0 → true; nota 6.5 → false. |
| T2.3 | `interfaces.rest.academico.CursoController`: GET `/cursos` e GET `/cursos/{id}` retornando `CursoResponseDTO` com `publicoAlvo` e `conhecimentosPrevios` (RF045) | Ihan | USA03 | **Critérios de Aceitação:**<br>• `CursoController` em `interfaces.rest.academico` usa `CursoRepository` (injeção via interface do domínio).<br>• GET `/cursos`: lista apenas cursos com status PUBLICADO. GET `/cursos/{id}`: inclui módulos, aulas, `publicoAlvo` e `conhecimentosPrevios` (RF045 — info-cards de nivelamento).<br>• Resposta via `CursoResponseDTO` em `interfaces.rest.academico` (nunca expõe entidade de domínio diretamente).<br>• GET `/cursos/{id}` inexistente → 404 com mensagem.<br><br>**Teste básico:** GET `/cursos` → 200 + array com cursos PUBLICADOS do seed. GET `/cursos/{id}` → JSON com módulos, aulas, publicoAlvo e conhecimentosPrevios. GET `/cursos/id-invalido` → 404. |
| T2.4 | `interfaces.rest.academico.MatriculaController`: POST `/matriculas` + GET `/matriculas/{id}/progresso` + `MatricularAlunoUseCase` em `application.academico` | Ihan | USA05 | **Critérios de Aceitação:**<br>• `MatricularAlunoUseCase` em `application.academico` orquestra: verifica saldo de `cursosExtras` do Aluno, cria Matricula, persiste via `MatriculaRepository`.<br>• POST `/matriculas`: delega para `MatricularAlunoUseCase`, retorna `MatriculaResponseDTO` (id, tituloCurso, status, moduloAtual, totalModulos).<br>• GET `/matriculas/{id}/progresso`: retorna estado de cada módulo (BLOQUEADO/EM_ANDAMENTO/CONCLUIDO) do aluno autenticado.<br>• Aluno só acessa suas próprias matrículas (validado via JWT claim `userId`).<br><br>**Teste básico:** POST `/matriculas` → 201 + `MatriculaResponseDTO`. GET `/matriculas/{id}/progresso` → estado dos módulos. Matrícula duplicada → 409. Acesso à matrícula de outro aluno → 403. |
| T2.5 | Seed de dados (CommandLineRunner): 3 cursos PUBLICADOS com módulos, aulas e usuário demo para apresentação | Ihago | USA03 | **Critérios de Aceitação:**<br>• Seed executado na inicialização via `@Profile("dev")` CommandLineRunner.<br>• Pelo menos 3 cursos com status PUBLICADO, 2 módulos cada, 3 aulas por módulo (incluindo 1 aula com ProjetoFinal associado).<br>• Usuário `demo_aluno` com `totalCursosConcluidos=11` e `saldoCursosExtras=33` para demonstração do upgrade Premium.<br>• Seed idempotente: verifica existência antes de inserir (não duplica em restarts).<br><br>**Teste básico:** App sobe → GET `/cursos` retorna 3 cursos. Login com `demo_aluno` → GET `/alunos/{id}/progresso` mostra 11 cursos concluídos e plano BASICO. |
| T2.6 | `CourseService` Angular (HttpClient) + página de catálogo exibindo cursos via `CursoResponseDTO` | Evelyn | USA03 | **Critérios de Aceitação:**<br>• `CourseService` em Angular chama GET `/cursos` e mapeia para interface TypeScript alinhada com `CursoResponseDTO`.<br>• Catálogo exibe cards com título, descrição, nivel e `publicoAlvo`.<br>• Loading state (spinner) durante fetch; erro de rede exibe mensagem sem quebrar a aplicação.<br><br>**Teste básico:** Catálogo carrega cards com dados do seed. Simular erro de rede → mensagem de erro exibida. Card exibe `publicoAlvo`. |
| T2.7 | Página de detalhe do curso: módulos, aulas, info-cards de nivelamento (`conhecimentosPrevios`/`publicoAlvo` — RF045) e botão de matrícula | Evelyn | USA04 | **Critérios de Aceitação:**<br>• Exibe título, descrição, carga horária, nível, `publicoAlvo` e `conhecimentosPrevios` como info-cards (RF045).<br>• Lista de módulos com suas aulas; tipo de aula (Teórica / Atividade / Projeto Final) identificado visualmente.<br>• Botão "Matricular" para alunos não matriculados → chama POST `/matriculas`. Após matrícula → botão "Continuar curso".<br><br>**Teste básico:** Clicar em curso → detalhe com info-cards visíveis. Click em Matricular → chamada POST `/matriculas` → botão muda para "Continuar". |
| T2.8 | `EnrollmentService` Angular: `enroll(cursoId)` e `getProgresso(matriculaId)` integrados com `MatriculaController` | Madu | USA05 | **Critérios de Aceitação:**<br>• `enroll(cursoId)` chama POST `/matriculas` e retorna `Observable<MatriculaResponse>`.<br>• `getProgresso(matriculaId)` chama GET `/matriculas/{id}/progresso` e retorna estado dos módulos.<br>• Erro 409 (duplicata) capturado e exibido ao usuário como mensagem informativa.<br><br>**Teste básico:** `enroll()` dispara POST `/matriculas` com body correto. `getProgresso()` dispara GET `/matriculas/{id}/progresso`. 409 → mensagem "Você já está matriculado neste curso" exibida. |

---

### 1.3 Progressão Acadêmica [academico]

| ID | Task | Owner | US | Critério de Aceitação + Teste Básico |
| :--- | :--- | :---: | :--- | :--- |
| T3.1 | `domain.academico.service.ProgressaoModularService`: implementa `podeAvancarModulo()` e `registrarConclusaoModulo()` com regras RN011/RN024 | Ihan | USA06 | **Critérios de Aceitação:**<br>• `ProgressaoModularService` em `domain.academico.service`; recebe Matricula e índice do módulo alvo.<br>• `podeAvancarModulo(ordemModulo)`: retorna false se módulo anterior não estiver CONCLUIDO (RN024); delega para `Matricula.podeAvancarModulo()`.<br>• `registrarConclusaoModulo(matricula, ordemModulo)`: atualiza `moduloAtual` na Matricula e persiste via `MatriculaRepository`.<br>• Módulo 0 (primeiro) sempre acessível. Módulos seguintes bloqueados até conclusão do anterior.<br><br>**Teste básico:** Acessar aula do módulo 2 sem concluir módulo 1 → `ProgressaoModularService` retorna false → endpoint responde 403. Concluir módulo 1 → `podeAvancarModulo(1)` retorna true. |
| T3.2 | `ConcluirModuloUseCase` em `application.academico` + endpoint POST `/matriculas/{id}/modulos/{ordem}/concluir` | Ihan | USA07 | **Critérios de Aceitação:**<br>• `ConcluirModuloUseCase` em `application.academico` orquestra: chama `ProgressaoModularService`, atualiza Matricula, emite evento `CursoConcluídoEvent` se último módulo.<br>• Endpoint POST `/matriculas/{id}/modulos/{ordem}/concluir` em `MatriculaController`.<br>• Retorno inclui percentual de progresso no curso e se módulo seguinte foi desbloqueado.<br>• Idempotente: re-executar para módulo já concluído não altera estado.<br><br>**Teste básico:** POST `/matriculas/{id}/modulos/0/concluir` → 200 + percentual. Re-executar → mesmo resultado sem side effects. POST para módulo bloqueado → 403. |
| T3.3 | Lógica de conclusão do curso: `Matricula.concluir(notaFinal)` chamado pelo `ConcluirModuloUseCase` ao fechar o último módulo | Ihan | USA07 | **Critérios de Aceitação:**<br>• Ao concluir o último módulo, `ConcluirModuloUseCase` chama `Matricula.concluir(notaFinal)` → status → CONCLUIDO ou REPROVADO.<br>• Nota final calculada como média das avaliações dos módulos e ProjetoFinal (se houver).<br>• `MatriculaRepository.countConcluidosByAluno()` atualiza `Aluno.totalCursosConcluidos`.<br>• Após concluir, `CursoConcluídoEvent` é publicado para que `RecompensaService` (contexto gamificacao) processe as recompensas sem acoplamento direto.<br><br>**Teste básico:** Concluir todos os módulos → `Matricula.status = CONCLUIDO`, notaFinal calculada. Nota > 7 e último módulo → `CursoConcluídoEvent` publicado (verificar listener disparado). |
| T3.4 | Página Angular do player de aula: embed de vídeo via iframe (YouTube), link para PDF/LINK, indicação do tipo de aula via `TipoConteudo` | Evelyn | USA06 | **Critérios de Aceitação:**<br>• `TipoConteudo` VIDEO → iframe YouTube incorporado. `TipoConteudo` PDF/LINK → botão "Abrir material" em nova aba.<br>• Tipo da aula (Teórica/Atividade/Projeto Final) exibido como badge na página.<br>• Botão "Marcar como concluída" visível apenas para aulas sem ProjetoFinal vinculado (aulas teóricas).<br>• Para aulas do tipo ATIVIDADE ou Projeto Final → redireciona para tela de submissão (E4).<br><br>**Teste básico:** Aula VIDEO → iframe renderizado com URL do seed. Aula PDF → botão de link visível. Aula Teórica → botão de conclusão presente. |
| T3.5 | UI Angular: estados visuais dos módulos (BLOQUEADO / EM_ANDAMENTO / CONCLUIDO) baseados no retorno de GET `/matriculas/{id}/progresso` | Evelyn | USA06 | **Critérios de Aceitação:**<br>• Módulo BLOQUEADO: ícone de cadeado, cor acinzentada, não-clicável.<br>• Módulo EM_ANDAMENTO: barra de progresso com percentual de aulas concluídas.<br>• Módulo CONCLUIDO: ícone de check verde.<br>• Estado atualizado via polling ou re-fetch após ação do usuário, sem reload de página.<br><br>**Teste básico:** Módulo 2 aparece bloqueado (cadeado) antes de concluir módulo 1. Após POST de conclusão do módulo 1 → módulo 2 fica EM_ANDAMENTO automaticamente. |
| T3.6 | Ação "Marcar aula como concluída": chama POST `/matriculas/{id}/modulos/{ordem}/concluir` e atualiza UI com feedback visual | Evelyn | USA07 | **Critérios de Aceitação:**<br>• Click no botão → spinner, botão desabilitado durante request.<br>• Sucesso → botão desabilitado permanentemente, barra de progresso do módulo atualizada.<br>• Se módulo completado, exibe toast "Módulo concluído! Próximo módulo desbloqueado."<br>• Erro de rede → toast de erro, botão re-habilitado.<br><br>**Teste básico:** Click → spinner → sucesso → barra atualizada. Re-click → botão não responde (desabilitado). Se último módulo do curso → toast de conclusão do curso aparece. |
| T3.7 | `ProgressService` Angular: `getProgresso(matriculaId)` e `concluirModulo(matriculaId, ordem)` integrados com `MatriculaController` | Madu | USA07 | **Critérios de Aceitação:**<br>• `getProgresso(matriculaId)` chama GET `/matriculas/{id}/progresso` e mapeia para interface `ProgressoDTO` Angular.<br>• `concluirModulo(matriculaId, ordem)` chama POST `/matriculas/{id}/modulos/{ordem}/concluir`.<br>• Estado de progresso cacheado localmente e atualizado após cada ação.<br><br>**Teste básico:** `concluirModulo()` dispara POST para rota correta. `getProgresso()` retorna mapa de módulos com estados corretos. Após conclusão, estado local atualizado imediatamente. |

---

### 1.4 Atividades e Submissão [academico]

| ID | Task | Owner | US | Critério de Aceitação + Teste Básico |
| :--- | :--- | :---: | :--- | :--- |
| T4.1 | `domain.academico.entity`: ProjetoFinal (submissão de arquivo — RF043) e Avaliacao (avaliação objetiva com gabarito — RF029/RF030) + enum `StatusProjeto` | Ihan | USA08 | **Critérios de Aceitação:**<br>• `ProjetoFinal` em `domain.academico.entity`: id UUID, matricula, urlArquivo, tutor (Professor), status `StatusProjeto` (AGUARDANDO/REVISAO/APROVADO), nota, feedback.<br>• `Avaliacao` em `domain.academico.entity`: id UUID, gabarito (`List<RespostaCorreta>` VO), questoes; método `corrigir(List<RespostaAluno>)` → double (RF030).<br>• `RespostaCorreta` como VO (Java Record) em `domain.academico.valueobject`.<br>• `ProjetoFinalRepository` interface em `domain.academico.repository`.<br><br>**Teste básico:** DDL gera tabelas projeto_final e avaliacao. ProjetoFinal criado com status AGUARDANDO. `Avaliacao.corrigir()` com gabarito correto retorna 10.0; com zero acertos retorna 0.0. |
| T4.2 | `interfaces.rest.academico.ProjetoFinalController`: POST `/projetos` — upload multipart de arquivo vinculado à Matricula (RF043) | Ihago | USA08 | **Critérios de Aceitação:**<br>• POST `/projetos` em `ProjetoFinalController` recebe `multipart/form-data` (arquivo + matriculaId + aulaId).<br>• Aceita PDF, DOCX, ZIP com máximo 10MB; valida content-type; retorna 400 se tipo inválido ou arquivo ausente.<br>• Arquivo salvo em diretório configurável; `urlArquivo` persistida no ProjetoFinal.<br>• Retorna 400 se aula referenciada não tiver ProjetoFinal como entrega esperada (validação no UseCase).<br><br>**Teste básico:** POST `/projetos` com PDF válido → 201 + `ProjetoFinalResponseDTO` com status AGUARDANDO. Upload de `.exe` → 400. Upload > 10MB → 400. Arquivo salvo em disco. |
| T4.3 | PATCH `/projetos/{id}/avaliar` — Professor atribui nota e feedback ao ProjetoFinal; valida autorização por curso | Ihago | USP02 | **Critérios de Aceitação:**<br>• PATCH `/projetos/{id}/avaliar` aceita body `{ nota: double, feedback: String }`.<br>• `AvaliarProjetoUseCase` em `application.academico` verifica que JWT role == PROFESSOR e professor é responsável pelo curso da matrícula.<br>• Persiste nota e feedback; muda `StatusProjeto` para APROVADO (nota >= 5) ou REVISAO. `corrigido=true`.<br>• Nota fora de 0-10 → 400. Professor de outro curso → 403.<br><br>**Teste básico:** PATCH com nota 8.5 e feedback → 200 + status APROVADO. Nota 3.0 → status REVISAO. Nota 11 → 400. Aluno tentando avaliar → 403. Professor de outro curso → 403. |
| T4.4 | GET `/cursos/{id}/projetos` — Professor lista ProjetosFinal pendentes de correção do curso; filtro por status | Ihan | USP01 | **Critérios de Aceitação:**<br>• Endpoint em `ProjetoFinalController` retorna lista de `ProjetoFinalResponseDTO` com: `nomeAluno`, `tituloAula`, `dataEnvio`, `urlArquivo`, `status`.<br>• Query param `?status=AGUARDANDO` filtra apenas pendentes.<br>• Acesso restrito a PROFESSOR responsável pelo curso (validado via JWT + repositório).<br><br>**Teste básico:** GET `/cursos/{id}/projetos` → 200 + lista. `?status=AGUARDANDO` → apenas pendentes. Aluno acessando → 403. Professor de outro curso → 403. |
| T4.5 | UI Angular: área de submissão de ProjetoFinal na tela de aula com drag-and-drop, status de correção e exibição de nota/feedback | Evelyn | USA08 | **Critérios de Aceitação:**<br>• Área de upload visível apenas em aulas que possuem ProjetoFinal como entrega esperada (verificado via tipo da aula no DTO).<br>• Exibe submissão anterior se existir: nome do arquivo, data de envio e status (AGUARDANDO/REVISAO/APROVADO).<br>• Após correção, exibe nota e feedback do professor.<br>• Drag-and-drop com indicador visual; valida extensão no cliente antes de enviar.<br><br>**Teste básico:** Aula com ProjetoFinal → área de upload visível. Aula teórica → área não exibida. Após upload → nome e data exibidos com status AGUARDANDO. Após avaliação → nota e feedback visíveis. |
| T4.6 | `SubmissionService` Angular: `uploadProjeto(matriculaId, aulaId, arquivo)` e `getProjetoStatus(projetoId)` integrados com `ProjetoFinalController` | Madu | USA08 | **Critérios de Aceitação:**<br>• `uploadProjeto()` monta `FormData` com arquivo, matriculaId e aulaId; chama POST `/projetos`.<br>• `getProjetoStatus(projetoId)` chama GET `/projetos/{id}` e retorna `Observable<ProjetoFinalResponse>`.<br>• Erros de validação (tipo inválido, tamanho) exibidos ao usuário.<br><br>**Teste básico:** `uploadProjeto()` dispara POST `/projetos` com FormData correto. `getProjetoStatus()` dispara GET para rota correta. Resposta 400 → mensagem de erro exibida na tela. |
| T4.7 | UI Angular (visão professor): listagem de ProjetosFinal pendentes e formulário de avaliação (nota + feedback) | Evelyn | USP01 | **Critérios de Aceitação:**<br>• Rota `/professor/projetos` exibe lista de projetos com: `nomeAluno`, aula, data, botão "Baixar arquivo" e botão "Avaliar".<br>• "Avaliar" abre formulário inline com campo nota (0-10) e textarea feedback.<br>• Submit chama PATCH `/projetos/{id}/avaliar`; item some da lista de pendentes após sucesso.<br>• Apenas usuário com role PROFESSOR consegue acessar a rota (AuthGuard com role check).<br><br>**Teste básico:** Login como professor → `/professor/projetos` lista pendentes. Click Avaliar → formulário aparece. Submit com nota 9 → projeto some da lista. Login como aluno → redirect de `/professor/projetos`. |

---

### 1.5 Gamificação e Recompensas [gamificacao]

| ID | Task | Owner | US | Critério de Aceitação + Teste Básico |
| :--- | :--- | :---: | :--- | :--- |
| T5.1 | `domain.gamificacao.service.RecompensaService`: listener de `CursoConcluídoEvent` que executa `processarFimDeCurso(Aluno, Matricula)` — RF002, RN002 | Ihago | USA10 | **Critérios de Aceitação:**<br>• `RecompensaService` em `domain.gamificacao.service` implementa listener de `CursoConcluídoEvent` (Spring `@EventListener`).<br>• `processarFimDeCurso()`: se `Matricula.verificarAprovacao()` → chama `Aluno.receberCursosExtras(3)` (RF002, RN002).<br>• Persiste Aluno atualizado via `AlunoRepository`. Registra `TransacaoMoeda` se aluno for Premium (RF007).<br>• Idempotente por `matriculaId`: não processa evento duplicado.<br><br>**Teste básico:** Publicar `CursoConcluídoEvent` com nota 8.0 → `Aluno.saldoCursosExtras += 3`. Nota 6.0 → sem alteração. Evento duplicado → sem processamento duplicado. |
| T5.2 | `domain.usuario.service.UpgradePlanoService` + `application.financeiro.UpgradeParaPremiumUseCase`: upgrade automático ao atingir 12 cursos (RN004) | Ihago | USA11 | **Critérios de Aceitação:**<br>• `UpgradePlanoService` em `domain.usuario.service`: `verificarEAplicarUpgrade(Aluno)` — se `totalCursosConcluidos >= 12` e `tipoPlano != PREMIUM` → chama `Aluno.fazerUpgradePremium()`.<br>• `RecompensaService` chama `UpgradePlanoService` após incrementar `totalCursosConcluidos`.<br>• `UpgradeParaPremiumUseCase` em `application.financeiro` persiste mudança e emite notificação via `infrastructure.messaging`.<br>• Aluno já PREMIUM → sem alteração (idempotente).<br><br>**Teste básico:** Aluno com 11 cursos conclui o 12º → `Aluno.tipoPlano = PREMIUM`. Aluno já PREMIUM → sem erro, sem mudança. Verificar que `Aluno.fazerUpgradePremium()` foi chamado exatamente uma vez. |
| T5.3 | Integração end-to-end do fluxo de recompensa: `CursoConcluídoEvent` conecta academico → gamificacao sem acoplamento direto (DDD — contextos separados) | Ihago | USA10 | **Critérios de Aceitação:**<br>• `ConcluirModuloUseCase` (academico) publica `CursoConcluídoEvent` via `ApplicationEventPublisher` do Spring.<br>• `RecompensaService` (gamificacao) escuta o evento via `@EventListener` em transação separada (`@TransactionalEventListener`).<br>• Nenhuma importação direta de classes do pacote gamificacao dentro do pacote academico (verificar no código).<br>• Em caso de falha no listener, evento é reenfileirado ou logado sem afetar a transação principal de conclusão.<br><br>**Teste básico:** Simular conclusão do último módulo → verificar `CursoConcluídoEvent` publicado (mock listener). Verificar ausência de import cruzado entre pacotes academico e gamificacao. |
| T5.4 | Seed de demo: perfil `demo_aluno` com 11 cursos concluídos para demonstração ao vivo do upgrade Premium | Ihago | USA11 | **Critérios de Aceitação:**<br>• `demo_aluno` com `totalCursosConcluidos=11`, `saldoCursosExtras=33`, `tipoPlano=BASICO`.<br>• Seed garante que `demo_aluno` está matriculado no 12º curso com todos os módulos anteriores ao último já concluídos.<br>• Um click na última ação da demo dispara upgrade PREMIUM ao vivo para a banca.<br><br>**Teste básico:** Login como `demo_aluno` → GET `/alunos/{id}/progresso` mostra plano BASICO e 11 cursos. Concluir último módulo do 12º curso → `tipoPlano` muda para PREMIUM (verificar no banco e no retorno da API). |

---

### 1.6 Dashboard do Aluno [usuario / gamificacao]

| ID | Task | Owner | US | Critério de Aceitação + Teste Básico |
| :--- | :--- | :---: | :--- | :--- |
| T6.1 | GET `/alunos/{id}/progresso` retornando `AlunoResponseDTO` + GET `/gamificacao/moedas` retornando `SaldoMoedasResponseDTO` (`GamificacaoController`) | Ihan | USA12 | **Critérios de Aceitação:**<br>• GET `/alunos/{id}/progresso` em `AlunoController` (`interfaces.rest.usuario`) retorna `AlunoResponseDTO`: id, nome, email, totalCursosConcluidos, saldoCursosExtras, saldoMoedas, tipoPlano.<br>• GET `/gamificacao/moedas` em `GamificacaoController` (`interfaces.rest.gamificacao`) retorna `SaldoMoedasResponseDTO`: saldo + historico de `TransacaoMoeda`.<br>• Acesso restrito ao próprio aluno (JWT claim `userId == pathVariable id`).<br><br>**Teste básico:** GET `/alunos/{id}/progresso` → 200 + `AlunoResponseDTO`. Acesso ao ID de outro aluno → 403. GET `/gamificacao/moedas` → histórico de transações. |
| T6.2 | Página Angular: Dashboard do aluno com cards de métricas (tipoPlano, saldo, cursos) e tabela de histórico de matrículas | Evelyn | USA12 | **Critérios de Aceitação:**<br>• Cards: tipo de plano com badge visual (BASICO cinza / PREMIUM dourado), `saldoCursosExtras`, `totalCursosConcluidos`.<br>• Tabela de matrículas com: título do curso, data de início, status e nota final.<br>• Badge PREMIUM destacado no header da aplicação se `tipoPlano == PREMIUM`.<br><br>**Teste básico:** Dashboard carrega dados via GET `/alunos/{id}/progresso`. Cards exibem valores corretos do seed. Aluno PREMIUM → badge dourado visível no header. |
| T6.3 | Toasts/notificações Angular: "+3 cursos desbloqueados" após conclusão com nota > 7 e "Parabéns, você é Premium!" no upgrade | Madu | USA10 | **Critérios de Aceitação:**<br>• Resposta do POST de conclusão de módulo inclui campo `recompensa` (null ou `{ tipo, quantidade }`) no DTO.<br>• Se recompensa presente → toast verde "+3 cursos desbloqueados".<br>• Se `tipoPlano` mudou para PREMIUM na resposta → toast dourado com destaque especial "Parabéns, você atingiu o plano Premium!".<br>• Toasts desaparecem após 5 segundos.<br><br>**Teste básico:** Concluir curso nota 8 (`demo_aluno`) → toast "+3 cursos" visível. Concluir 12º curso → toast Premium visível e badge no header atualiza imediatamente. |

---

## 2. Planejamento das Sprints

3 sprints de 1 semana cada. Bounded contexts separados por sprint para minimizar conflitos de merge e facilitar revisão de código cruzado.

---

### Sprint 1 — Autenticação e Catálogo

**Período:** 05/05 (seg) → 11/05 (dom)
**Bounded Contexts:** usuario + academico (Epics E1 + E2)
**Meta:** Aluno consegue se cadastrar, fazer login, visualizar o catálogo com info-cards de nivelamento e se matricular em um curso.

| Epic | Task | Descrição | Owner | Prior. | US + Critério + Teste |
| :--- | :--- | :--- | :---: | :---: | :--- |
| E1 | T1.1 | Entidades Pessoa (abstract), Aluno, Professor, Administrador em `domain.usuario.entity` com herança JPA + Value Objects Email e NomeCompleto como Java Records | Ihago | Must Have | **US-A01** \| AC: Classe abstrata Pessoa com id UUID em `domain.usuario.entity`; herança `@Inheritance(JOINED)`. (+ 4 critérios)<br>**Teste:** Aplicação sobe sem erros; DDL gera tabelas pessoa/aluno/professor/administrador com FK de herança. Persistir Aluno com Email VO → registro salvo; email inválido → exceção lançada no construtor do VO. |
| E1 | T1.2 | `infrastructure.security`: JwtAuthFilter, SecurityConfig com roles ALUNO/PROFESSOR/ADMIN, e geração/validação de token JWT | Ihago | Must Have | **US-A02** \| AC: `JwtAuthFilter` em `infrastructure.security` extrai e valida token JWT em toda requisição. (+ 3 critérios)<br>**Teste:** POST `/auth/login` → 200 + JWT. Request em rota protegida sem token → 401. Request com token de ALUNO em rota de ADMIN → 403. |
| E1 | T1.3 | API REST: POST `/auth/register` e POST `/auth/login` em `interfaces.rest.usuario.AuthController` + `AuthUseCase` em `application.usuario` | Ihago | Must Have | **US-A01** \| AC: `AuthController` em `interfaces.rest.usuario` delega para `AuthUseCase` em `application.usuario`. (+ 3 critérios)<br>**Teste:** POST `/auth/register` com dados válidos → 201. E-mail duplicado → 409. POST `/auth/login` correto → 200 + token. Senha errada → 401 com mensagem genérica. |
| E1 | T1.4 | Angular: módulo de roteamento, AuthGuard usando JWT claims, interceptor HTTP que injeta Bearer token | Evelyn | Must Have | **US-A02** \| AC: `AuthGuard` verifica token e role via `AuthService`; redireciona para `/login` se ausente ou expirado. (+ 2 critérios)<br>**Teste:** Acessar `/catalogo` sem login → redirect `/login`. Após login, todas requests HTTP têm header Bearer. Token expirado → redirect `/login` automático. |
| E1 | T1.5 | `AuthService` Angular: armazenamento de token, extração de claims JWT, métodos `isLoggedIn()` e `getCurrentUser()` | Madu | Must Have | **US-A02** \| AC: Token persistido em `localStorage`. `isLoggedIn()` verifica presença e validade (exp claim). (+ 2 critérios)<br>**Teste:** Login → token em `localStorage`. Reload → ainda autenticado. `logout()` → token removido. Token expirado → `isLoggedIn()` retorna false. |
| E1 | T1.6 | Páginas Angular: Login e Cadastro com formulários reativos, validação inline e integração com AuthController | Evelyn | Must Have | **US-A01** \| AC: Formulários reativos validam: nome obrigatório, email formato válido, senha mínima 6 caracteres. (+ 2 critérios)<br>**Teste:** Submit com campos vazios → erros inline visíveis. Submit válido → chamada API. Resposta de e-mail duplicado → mensagem de erro exibida. |
| E2 | T2.1 | `domain.academico.entity`: Curso (Aggregate Root), Modulo, Aula + enums StatusCurso, ModalidadeCurso, TipoConteudo em `domain.academico.enums` + VO NivelCurso | Ihan | Must Have | **US-A03** \| AC: Curso como Aggregate Root com métodos `adicionarModulo()` e `aceitaNovasMatriculas()`; atributos: `publicoAlvo`, `conhecimentosPrevios`, `status` (StatusCurso), `modalidade` (ModalidadeCurso), `maxAlunos`, `minAlunos`. (+ 4 critérios)<br>**Teste:** DDL gera tabelas curso/modulo/aula com FKs corretas. Seed cria curso com 2 módulos e 3 aulas. `Curso.aceitaNovasMatriculas()` retorna false se inscritos >= maxAlunos. |
| E2 | T2.2 | `domain.academico.entity`: Matricula (Aggregate Root) com métodos de domínio `podeAvancarModulo()` e `concluir()` + `MatriculaRepository` interface | Ihan | Must Have | **US-A05** \| AC: Matricula como Aggregate Root com: `aluno` (UUID ref), `curso` (UUID ref), `status` (StatusMatricula), `moduloAtual`, `notaFinal`. (+ 3 critérios)<br>**Teste:** Matricular aluno → status EM_ANDAMENTO. Tentar segunda matrícula no mesmo curso → 409. `Matricula.verificarAprovacao()` com nota 8.0 → true; nota 6.5 → false. |
| E2 | T2.3 | `interfaces.rest.academico.CursoController`: GET `/cursos` e GET `/cursos/{id}` retornando `CursoResponseDTO` com `publicoAlvo` e `conhecimentosPrevios` (RF045) | Ihan | Must Have | **US-A03** \| AC: `CursoController` em `interfaces.rest.academico` usa `CursoRepository` (injeção via interface do domínio). (+ 3 critérios)<br>**Teste:** GET `/cursos` → 200 + array com cursos PUBLICADOS do seed. GET `/cursos/{id}` → JSON com módulos, aulas, publicoAlvo e conhecimentosPrevios. GET `/cursos/id-invalido` → 404. |
| E2 | T2.4 | `interfaces.rest.academico.MatriculaController`: POST `/matriculas` + GET `/matriculas/{id}/progresso` + `MatricularAlunoUseCase` em `application.academico` | Ihan | Must Have | **US-A05** \| AC: `MatricularAlunoUseCase` em `application.academico` orquestra: verifica saldo de `cursosExtras` do Aluno, cria Matricula, persiste via `MatriculaRepository`. (+ 3 critérios)<br>**Teste:** POST `/matriculas` → 201 + `MatriculaResponseDTO`. GET `/matriculas/{id}/progresso` → estado dos módulos. Matrícula duplicada → 409. Acesso à matrícula de outro aluno → 403. |
| E2 | T2.5 | Seed de dados (CommandLineRunner): 3 cursos PUBLICADOS com módulos, aulas e usuário demo para apresentação | Ihago | Must Have | **US-A03** \| AC: Seed executado na inicialização via `@Profile("dev")` CommandLineRunner. (+ 3 critérios)<br>**Teste:** App sobe → GET `/cursos` retorna 3 cursos. Login com `demo_aluno` → GET `/alunos/{id}/progresso` mostra 11 cursos concluídos e plano BASICO. |
| E2 | T2.6 | `CourseService` Angular (HttpClient) + página de catálogo exibindo cursos via `CursoResponseDTO` | Evelyn | Must Have | **US-A03** \| AC: `CourseService` em Angular chama GET `/cursos` e mapeia para interface TypeScript alinhada com `CursoResponseDTO`. (+ 2 critérios)<br>**Teste:** Catálogo carrega cards com dados do seed. Simular erro de rede → mensagem de erro exibida. Card exibe `publicoAlvo`. |
| E2 | T2.7 | Página de detalhe do curso: módulos, aulas, info-cards de nivelamento (`conhecimentosPrevios`/`publicoAlvo` — RF045) e botão de matrícula | Evelyn | Must Have | **US-A04** \| AC: Exibe título, descrição, carga horária, nível, `publicoAlvo` e `conhecimentosPrevios` como info-cards (RF045). (+ 2 critérios)<br>**Teste:** Clicar em curso → detalhe com info-cards visíveis. Click em Matricular → chamada POST `/matriculas` → botão muda para "Continuar". |
| E2 | T2.8 | `EnrollmentService` Angular: `enroll(cursoId)` e `getProgresso(matriculaId)` integrados com `MatriculaController` | Madu | Must Have | **US-A05** \| AC: `enroll(cursoId)` chama POST `/matriculas` e retorna `Observable<MatriculaResponse>`. (+ 2 critérios)<br>**Teste:** `enroll()` dispara POST `/matriculas` com body correto. `getProgresso()` dispara GET `/matriculas/{id}/progresso`. 409 → mensagem "Você já está matriculado neste curso" exibida. |

---

### Sprint 2 — Progressão e Atividades

**Período:** 12/05 (seg) → 18/05 (dom)
**Bounded Contexts:** academico — Epics E3 + E4
**Meta:** Aluno percorre o curso em sequência (`ProgressaoModularService` + RN011/RN024), envia ProjetoFinal via upload, e professor avalia via `AvaliarProjetoUseCase`. `ConcluirModuloUseCase` publica `CursoConcluídoEvent`.

| Epic | Task | Descrição | Owner | Prior. | US + Critério + Teste |
| :--- | :--- | :--- | :---: | :---: | :--- |
| E3 | T3.1 | `domain.academico.service.ProgressaoModularService`: implementa `podeAvancarModulo()` e `registrarConclusaoModulo()` com regras RN011/RN024 | Ihan | Must Have | **US-A06** \| AC: `ProgressaoModularService` em `domain.academico.service`; recebe Matricula e índice do módulo alvo. (+ 3 critérios)<br>**Teste:** Acessar aula do módulo 2 sem concluir módulo 1 → `ProgressaoModularService` retorna false → endpoint responde 403. Concluir módulo 1 → `podeAvancarModulo(1)` retorna true. |
| E3 | T3.2 | `ConcluirModuloUseCase` em `application.academico` + endpoint POST `/matriculas/{id}/modulos/{ordem}/concluir` | Ihan | Must Have | **US-A07** \| AC: `ConcluirModuloUseCase` em `application.academico` orquestra: chama `ProgressaoModularService`, atualiza Matricula, emite evento `CursoConcluídoEvent` se último módulo. (+ 3 critérios)<br>**Teste:** POST `/matriculas/{id}/modulos/0/concluir` → 200 + percentual. Re-executar → mesmo resultado sem side effects. POST para módulo bloqueado → 403. |
| E3 | T3.3 | Lógica de conclusão do curso: `Matricula.concluir(notaFinal)` chamado pelo `ConcluirModuloUseCase` ao fechar o último módulo | Ihan | Must Have | **US-A07** \| AC: Ao concluir o último módulo, `ConcluirModuloUseCase` chama `Matricula.concluir(notaFinal)` → status → CONCLUIDO ou REPROVADO. (+ 3 critérios)<br>**Teste:** Concluir todos os módulos → `Matricula.status = CONCLUIDO`, notaFinal calculada. Nota > 7 e último módulo → `CursoConcluídoEvent` publicado (verificar listener disparado). |
| E3 | T3.4 | Página Angular do player de aula: embed de vídeo via iframe (YouTube), link para PDF/LINK, indicação do tipo de aula via `TipoConteudo` | Evelyn | Must Have | **US-A06** \| AC: `TipoConteudo` VIDEO → iframe YouTube incorporado. `TipoConteudo` PDF/LINK → botão "Abrir material" em nova aba. (+ 3 critérios)<br>**Teste:** Aula VIDEO → iframe renderizado com URL do seed. Aula PDF → botão de link visível. Aula Teórica → botão de conclusão presente. |
| E3 | T3.5 | UI Angular: estados visuais dos módulos (BLOQUEADO / EM_ANDAMENTO / CONCLUIDO) baseados no retorno de GET `/matriculas/{id}/progresso` | Evelyn | Must Have | **US-A06** \| AC: Módulo BLOQUEADO: ícone de cadeado, cor acinzentada, não-clicável. (+ 3 critérios)<br>**Teste:** Módulo 2 aparece bloqueado (cadeado) antes de concluir módulo 1. Após POST de conclusão do módulo 1 → módulo 2 fica EM_ANDAMENTO automaticamente. |
| E3 | T3.6 | Ação "Marcar aula como concluída": chama POST `/matriculas/{id}/modulos/{ordem}/concluir` e atualiza UI com feedback visual | Evelyn | Must Have | **US-A07** \| AC: Click no botão → spinner, botão desabilitado durante request. (+ 3 critérios)<br>**Teste:** Click → spinner → sucesso → barra atualizada. Re-click → botão não responde (desabilitado). Se último módulo do curso → toast de conclusão do curso aparece. |
| E3 | T3.7 | `ProgressService` Angular: `getProgresso(matriculaId)` e `concluirModulo(matriculaId, ordem)` integrados com `MatriculaController` | Madu | Must Have | **US-A07** \| AC: `getProgresso(matriculaId)` chama GET `/matriculas/{id}/progresso` e mapeia para interface `ProgressoDTO` Angular. (+ 2 critérios)<br>**Teste:** `concluirModulo()` dispara POST para rota correta. `getProgresso()` retorna mapa de módulos com estados corretos. Após conclusão, estado local atualizado imediatamente. |
| E4 | T4.1 | `domain.academico.entity`: ProjetoFinal (submissão de arquivo — RF043) e Avaliacao (avaliação objetiva com gabarito — RF029/RF030) + enum `StatusProjeto` | Ihan | Must Have | **US-A08** \| AC: `ProjetoFinal` em `domain.academico.entity`: id UUID, matricula, urlArquivo, tutor (Professor), status `StatusProjeto` (AGUARDANDO/REVISAO/APROVADO), nota, feedback. (+ 3 critérios)<br>**Teste:** DDL gera tabelas projeto_final e avaliacao. ProjetoFinal criado com status AGUARDANDO. `Avaliacao.corrigir()` com gabarito correto retorna 10.0; com zero acertos retorna 0.0. |
| E4 | T4.2 | `interfaces.rest.academico.ProjetoFinalController`: POST `/projetos` — upload multipart de arquivo vinculado à Matricula (RF043) | Ihago | Must Have | **US-A08** \| AC: POST `/projetos` em `ProjetoFinalController` recebe `multipart/form-data` (arquivo + matriculaId + aulaId). (+ 3 critérios)<br>**Teste:** POST `/projetos` com PDF válido → 201 + `ProjetoFinalResponseDTO` com status AGUARDANDO. Upload de `.exe` → 400. Upload > 10MB → 400. Arquivo salvo em disco. |
| E4 | T4.3 | PATCH `/projetos/{id}/avaliar` — Professor atribui nota e feedback ao ProjetoFinal; valida autorização por curso | Ihago | Must Have | **US-P02** \| AC: PATCH `/projetos/{id}/avaliar` aceita body `{ nota: double, feedback: String }`. (+ 3 critérios)<br>**Teste:** PATCH com nota 8.5 e feedback → 200 + status APROVADO. Nota 3.0 → status REVISAO. Nota 11 → 400. Aluno tentando avaliar → 403. Professor de outro curso → 403. |
| E4 | T4.4 | GET `/cursos/{id}/projetos` — Professor lista ProjetosFinal pendentes de correção do curso; filtro por status | Ihan | Must Have | **US-P01** \| AC: Endpoint em `ProjetoFinalController` retorna lista de `ProjetoFinalResponseDTO` com: `nomeAluno`, `tituloAula`, `dataEnvio`, `urlArquivo`, `status`. (+ 2 critérios)<br>**Teste:** GET `/cursos/{id}/projetos` → 200 + lista. `?status=AGUARDANDO` → apenas pendentes. Aluno acessando → 403. Professor de outro curso → 403. |
| E4 | T4.5 | UI Angular: área de submissão de ProjetoFinal na tela de aula com drag-and-drop, status de correção e exibição de nota/feedback | Evelyn | Must Have | **US-A08** \| AC: Área de upload visível apenas em aulas que possuem ProjetoFinal como entrega esperada (verificado via tipo da aula no DTO). (+ 3 critérios)<br>**Teste:** Aula com ProjetoFinal → área de upload visível. Aula teórica → área não exibida. Após upload → nome e data exibidos com status AGUARDANDO. Após avaliação → nota e feedback visíveis. |
| E4 | T4.6 | `SubmissionService` Angular: `uploadProjeto(matriculaId, aulaId, arquivo)` e `getProjetoStatus(projetoId)` integrados com `ProjetoFinalController` | Madu | Must Have | **US-A08** \| AC: `uploadProjeto()` monta `FormData` com arquivo, matriculaId e aulaId; chama POST `/projetos`. (+ 2 critérios)<br>**Teste:** `uploadProjeto()` dispara POST `/projetos` com FormData correto. `getProjetoStatus()` dispara GET para rota correta. Resposta 400 → mensagem de erro exibida na tela. |
| E4 | T4.7 | UI Angular (visão professor): listagem de ProjetosFinal pendentes e formulário de avaliação (nota + feedback) | Evelyn | Must Have | **US-P01** \| AC: Rota `/professor/projetos` exibe lista de projetos com: `nomeAluno`, aula, data, botão "Baixar arquivo" e botão "Avaliar". (+ 3 critérios)<br>**Teste:** Login como professor → `/professor/projetos` lista pendentes. Click Avaliar → formulário aparece. Submit com nota 9 → projeto some da lista. Login como aluno → redirect de `/professor/projetos`. |

---

### Sprint 3 — Gamificação, Dashboard e Demo

**Período:** 19/05 (seg) → 25/05 (dom) — 23-25/05 reservados para testes e ensaio da demo
**Bounded Contexts:** gamificacao + usuario — Epics E5 + E6
**Meta:** `RecompensaService` processa `CursoConcluídoEvent` (+3 cursos, RN002); `UpgradePlanoService` faz upgrade automático para PREMIUM ao atingir 12 cursos (RN004); dashboard exibe `AlunoResponseDTO` e `SaldoMoedasResponseDTO`. Demo end-to-end com `demo_aluno` funcionando ao vivo.

| Epic | Task | Descrição | Owner | Prior. | US + Critério + Teste |
| :--- | :--- | :--- | :---: | :---: | :--- |
| E5 | T5.1 | `domain.gamificacao.service.RecompensaService`: listener de `CursoConcluídoEvent` que executa `processarFimDeCurso(Aluno, Matricula)` — RF002, RN002 | Ihago | Must Have | **US-A10** \| AC: `RecompensaService` em `domain.gamificacao.service` implementa listener de `CursoConcluídoEvent` (Spring `@EventListener`). (+ 3 critérios)<br>**Teste:** Publicar `CursoConcluídoEvent` com nota 8.0 → `Aluno.saldoCursosExtras += 3`. Nota 6.0 → sem alteração. Evento duplicado → sem processamento duplicado. |
| E5 | T5.2 | `domain.usuario.service.UpgradePlanoService` + `application.financeiro.UpgradeParaPremiumUseCase`: upgrade automático ao atingir 12 cursos (RN004) | Ihago | Must Have | **US-A11** \| AC: `UpgradePlanoService` em `domain.usuario.service`: `verificarEAplicarUpgrade(Aluno)` — se `totalCursosConcluidos >= 12` e `tipoPlano != PREMIUM` → chama `Aluno.fazerUpgradePremium()`. (+ 3 critérios)<br>**Teste:** Aluno com 11 cursos conclui o 12º → `Aluno.tipoPlano = PREMIUM`. Aluno já PREMIUM → sem erro, sem mudança. Verificar que `Aluno.fazerUpgradePremium()` foi chamado exatamente uma vez. |
| E5 | T5.3 | Integração end-to-end do fluxo de recompensa: `CursoConcluídoEvent` conecta academico → gamificacao sem acoplamento direto (DDD — contextos separados) | Ihago | Must Have | **US-A10** \| AC: `ConcluirModuloUseCase` (academico) publica `CursoConcluídoEvent` via `ApplicationEventPublisher` do Spring. (+ 3 critérios)<br>**Teste:** Simular conclusão do último módulo → verificar `CursoConcluídoEvent` publicado (mock listener). Verificar ausência de import cruzado entre pacotes academico e gamificacao. |
| E5 | T5.4 | Seed de demo: perfil `demo_aluno` com 11 cursos concluídos para demonstração ao vivo do upgrade Premium | Ihago | Must Have | **US-A11** \| AC: `demo_aluno` com `totalCursosConcluidos=11`, `saldoCursosExtras=33`, `tipoPlano=BASICO`. (+ 2 critérios)<br>**Teste:** Login como `demo_aluno` → GET `/alunos/{id}/progresso` mostra plano BASICO e 11 cursos. Concluir último módulo do 12º curso → `tipoPlano` muda para PREMIUM (verificar no banco e no retorno da API). |
| E6 | T6.1 | GET `/alunos/{id}/progresso` retornando `AlunoResponseDTO` + GET `/gamificacao/moedas` retornando `SaldoMoedasResponseDTO` (`GamificacaoController`) | Ihan | Should Have | **US-A12** \| AC: GET `/alunos/{id}/progresso` em `AlunoController` (`interfaces.rest.usuario`) retorna `AlunoResponseDTO`: id, nome, email, totalCursosConcluidos, saldoCursosExtras, saldoMoedas, tipoPlano. (+ 2 critérios)<br>**Teste:** GET `/alunos/{id}/progresso` → 200 + `AlunoResponseDTO`. Acesso ao ID de outro aluno → 403. GET `/gamificacao/moedas` → histórico de transações. |
| E6 | T6.2 | Página Angular: Dashboard do aluno com cards de métricas (tipoPlano, saldo, cursos) e tabela de histórico de matrículas | Evelyn | Should Have | **US-A12** \| AC: Cards: tipo de plano com badge visual (BASICO cinza / PREMIUM dourado), `saldoCursosExtras`, `totalCursosConcluidos`. (+ 2 critérios)<br>**Teste:** Dashboard carrega dados via GET `/alunos/{id}/progresso`. Cards exibem valores corretos do seed. Aluno PREMIUM → badge dourado visível no header. |
| E6 | T6.3 | Toasts/notificações Angular: "+3 cursos desbloqueados" após conclusão com nota > 7 e "Parabéns, você é Premium!" no upgrade | Madu | Should Have | **US-A10** \| AC: Resposta do POST de conclusão de módulo inclui campo `recompensa` (null ou `{ tipo, quantidade }`) no DTO. (+ 3 critérios)<br>**Teste:** Concluir curso nota 8 (`demo_aluno`) → toast "+3 cursos" visível. Concluir 12º curso → toast Premium visível e badge no header atualiza imediatamente. |

#### Buffer de Demo (23-25/05)

- Testar fluxo end-to-end com `demo_aluno` (11 cursos pré-concluídos → 12º curso → upgrade PREMIUM ao vivo).
- Verificar ausência de import cruzado entre pacotes academico e gamificacao (separação de contextos DDD).
- Confirmar que `CursoConcluídoEvent` é processado em transação separada (`@TransactionalEventListener`) sem afetar a transação principal.
- Ensaiar roteiro: cadastro → catálogo → matrícula → progressão → upload ProjetoFinal → avaliação professor → recompensa → upgrade Premium.

---

## 3. User Stories

### 3.1 Persona: Aluno

| ID | User Story | Critérios de Aceitação |
| :--- | :--- | :--- |
| USA01 | Como aluno, quero me cadastrar com nome, e-mail e senha, para criar minha conta e acessar o catálogo de cursos. | 1. Formulário valida: nome obrigatório, e-mail válido via VO Email no backend (regex), senha mínima de 6 caracteres.<br>2. E-mail duplicado retorna 409 com mensagem "E-mail já cadastrado".<br>3. Senha armazenada com BCrypt (nunca em texto plano).<br>4. Sucesso → redirect para `/login`. |
| USA02 | Como aluno, quero fazer login com meu e-mail e senha, para acessar minha conta e continuar meu aprendizado. | 5. Login retorna JWT com claims userId (UUID), email e role ALUNO.<br>6. Token armazenado em `localStorage`; ao recarregar a página, sessão é mantida.<br>7. Credenciais inválidas → 401 com mensagem genérica (sem revelar qual campo está errado).<br>8. `logout()` remove token e redireciona para `/login`. |
| USA03 | Como aluno, quero ver o catálogo de cursos disponíveis com suas informações, para explorar opções e escolher o que desejo estudar. | 9. Catálogo exibe apenas cursos com `StatusCurso.PUBLICADO`.<br>10. Cards com: título, descrição, nível (`NivelCurso` VO) e público-alvo.<br>11. Spinner durante fetch; erro de rede exibe mensagem sem quebrar a aplicação. |
| USA04 | Como aluno, quero ver os detalhes de um curso antes de me matricular, para entender o conteúdo e os pré-requisitos antes de decidir. | 12. Página exibe info-cards de nivelamento: `publicoAlvo` e `conhecimentosPrevios` (RF045 — RN026: consultivos, não bloqueiam matrícula).<br>13. Lista de módulos e aulas com indicação do tipo de conteúdo e se exige entrega de ProjetoFinal.<br>14. Botão "Matricular" visível para alunos não matriculados. |
| USA05 | Como aluno, quero me matricular em um curso, para ter acesso ao conteúdo e iniciar meu aprendizado. | 15. `MatricularAlunoUseCase` verifica saldo de `cursosExtras` se aplicável e cria Matricula com status EM_ANDAMENTO.<br>16. Após matrícula, botão muda para "Continuar curso" e primeiro módulo é desbloqueado.<br>17. Tentar matricular-se duas vezes → 409 com mensagem informativa. |
| USA06 | Como aluno, quero acessar aulas em sequência com módulos bloqueados até concluir o anterior, para garantir que estou aprendendo na ordem correta. | 18. Módulos bloqueados exibem cadeado e são não-clicáveis (`ProgressaoModularService` — RN011/RN024).<br>19. Ao concluir todas as aulas obrigatórias e ProjetoFinal de um módulo, o próximo é desbloqueado automaticamente.<br>20. Aulas VIDEO exibidas em iframe; PDF/LINK exibidos como botão de acesso externo. |
| USA07 | Como aluno, quero registrar minha conclusão em aulas teóricas, para acompanhar meu progresso e avançar no módulo. | 21. Botão "Marcar como concluída" presente apenas em aulas sem ProjetoFinal vinculado.<br>22. Click chama POST `/matriculas/{id}/modulos/{ordem}/concluir` via `ConcluirModuloUseCase`.<br>23. Barra de progresso do módulo atualizada; botão desabilitado após sucesso.<br>24. Re-clicar (idempotência) não duplica registro. |
| USA08 | Como aluno, quero fazer upload de arquivo em atividades e na prova final, para entregar meu trabalho para avaliação do professor. | 25. Interface de upload visível apenas em aulas que requerem ProjetoFinal.<br>26. Aceita PDF, DOCX, ZIP (máx 10MB); validação de tipo no cliente e no servidor.<br>27. Após envio, exibe nome do arquivo, data e status AGUARDANDO.<br>28. POST `/projetos` cria ProjetoFinal vinculado à Matricula (RF043). |
| USA09 | Como aluno, quero ver a nota e o feedback da minha submissão após correção, para entender meu desempenho e o que devo melhorar. | 29. Status AGUARDANDO enquanto professor não avaliar.<br>30. Após PATCH `/projetos/{id}/avaliar`, nota e feedback exibidos na tela da aula.<br>31. `StatusProjeto`: APROVADO (nota >= 5) ou REVISAO; exibido com badge colorido. |
| USA10 | Como aluno, quero receber +3 cursos extras ao concluir um curso com nota acima de 7,0, para ser recompensado pelo meu bom desempenho. | 32. `RecompensaService.processarFimDeCurso()` acionado via `CursoConcluídoEvent` após `Matricula.concluir()`.<br>33. Se `Matricula.verificarAprovacao()` → `Aluno.receberCursosExtras(3)` → `saldoCursosExtras += 3` (RN002).<br>34. Toast "+3 cursos desbloqueados" exibido no frontend.<br>35. Processamento idempotente por `matriculaId`. |
| USA11 | Como aluno, quero ter meu plano automaticamente atualizado para Premium ao completar 12 cursos, para usufruir dos benefícios Premium como reconhecimento da minha dedicação. | 36. `UpgradePlanoService.verificarEAplicarUpgrade()` chamado após incremento de `totalCursosConcluidos`.<br>37. Se total >= 12 e `tipoPlano != PREMIUM` → `Aluno.fazerUpgradePremium()` → `tipoPlano = PREMIUM` (RN004).<br>38. Toast especial "Parabéns, você atingiu o plano Premium!" exibido; badge PREMIUM aparece no header.<br>39. Upgrade idempotente: aluno já PREMIUM → sem alteração. |
| USA12 | Como aluno, quero visualizar meu dashboard com saldo, plano e histórico, para acompanhar minha evolução na plataforma. | 40. `AlunoResponseDTO` retornado por GET `/alunos/{id}/progresso` contém: `tipoPlano`, `saldoCursosExtras`, `totalCursosConcluidos`, `saldoMoedas`.<br>41. Tabela de matrículas com título do curso, data de início, status e nota final.<br>42. Badge visual diferenciado para plano PREMIUM vs BASICO. |

---

### 3.2 Persona: Professor

| ID | User Story | Critérios de Aceitação |
| :--- | :--- | :--- |
| USP01 | Como professor, quero ver a lista de ProjetosFinal pendentes de correção dos meus cursos, para saber quais trabalhos precisam de avaliação. | 43. GET `/cursos/{id}/projetos` retorna `ProjetoFinalResponseDTO` com: `nomeAluno`, `tituloAula`, `dataEnvio`, `urlArquivo`, `status`.<br>44. Filtro `?status=AGUARDANDO` exibe apenas os pendentes.<br>45. Acesso restrito ao professor responsável pelo curso (validado via JWT + `AvaliarProjetoUseCase`). |
| USP02 | Como professor, quero atribuir nota e feedback a cada ProjetoFinal, para avaliar os trabalhos e dar retorno construtivo aos alunos. | 46. PATCH `/projetos/{id}/avaliar` aceita nota (0-10) e feedback textual.<br>47. Nota fora de 0-10 → 400. Professor de outro curso → 403.<br>48. `StatusProjeto` muda para APROVADO (>= 5) ou REVISAO (< 5).<br>49. `AvaliarProjetoUseCase` persiste e notifica aluno via `infrastructure.messaging`. |
| USP03 | Como professor, quero que a nota do ProjetoFinal impacte o cálculo da nota final do aluno, para garantir que a avaliação prática reflita corretamente na aprovação no curso. | 50. `ConcluirModuloUseCase` só marca módulo como CONCLUIDO se ProjetoFinal associado tiver `StatusProjeto.APROVADO` (RN023/RN024).<br>51. Nota do ProjetoFinal compõe a média final calculada em `Matricula.concluir(notaFinal)`.<br>52. Matricula recebe status CONCLUIDO (nota >= 5) ou REPROVADO. |
